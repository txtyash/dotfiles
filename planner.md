# Dotfiles

- In neovim if there are no suggestions then <C-n> & <C-p> can be used for line up/down.
- Build a custom Telescope buffer command that shows buffers only either in the current directory or tab
- Try to map function keys like emoji, asus crate, etc.
- Telescope buffers does not sort files by last opened
- Telescope binding to delete buffers
- 3 finger tap and keyboard shortcut to pause media
- Set spell for markdown files
- Shortcut for
  - copy entire page
- How do I convert my dotfiles also to a devshell so that I have lua lsp in the .config directory
- Remove focus follows cursor from niri
- Use prettier for formatting files
- Use an AI plugin to use Gemini for writing blog posts
- configure api key management using sops
- pressing `o`/`O` in normal mode should insert an empty line without going into insert mode.
- configure ways to sync vimiumc configurations and brave bookmarks with dotfiles
- customize vimiumc heads up and hints
- Drag and drop yazi
- `installerIso` flake output parameter for NixOS
- customize github profile README.md to show shenanigans like the back to the future flying car GIF
- use a single scratchpad file for all todos and everything
- Add default branch name as master for git
- Dms dark theme toggle should also toggle gsettings dark mode
- Telekasten bugs:
  - When default name for new files is set to uuid in the config, it still prompts for file names
  - After creating a new file with default names like uuid, the confirmation has an 'A' appended to the uuid

## Dank Material Shell

- Right click on an app in the app tray does not have an option to maximize the app
- Note pad does not have markdown support
- No scan for wifi networks button
- Wallpaper discovery plugin cannot set wallpaper and always reloads all wallpapers on scroll; does not auto hide panel on click outside;
- Notepad does not hide on click-outside;
- When launcher/clipboard is opened, user is unable to write in the notepad
- I just noticed the media controls go out of sync with a yt vide. the video was playing but the controls displayed paused.
- sometimes cursor does not appear no matter what. similar tto 6 maybe.
- multiple multimedia is not shown in the media controls. like 2 yt videos simultaneously playing.
- Dank bar should have a help widget by default to view keybindings etc.
- Which key for window managers would be great!
- The CPU & Memory icons in the dank bar should be merged. They both open the same widget. The cards have different colors independent of what criteria the list is sorted by. The cards should have same color. The focused card should have a blue color.
